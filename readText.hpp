//phys1610hw3

#ifndef readText__hpp
#define readText__hpp

#include <stdio.h>
#include <rarray>
#include <iostream>

/// @brief function to read from a text file and print the contents to the console
/// @param length the length of a 
void read_text_file(int length , std::string filename, int numReads, int initAnts, int outputInc);

#endif /* readText_hpp */
